"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getThemeSelector = void 0;
const strategy_1 = require("../theme/strategy");
const errors_1 = require("../errors");
const lodash_1 = __importDefault(require("lodash"));
/**
 * Gets the selector for the gien theme.
 */
function getThemeSelector(manager, theme) {
    var _a, _b;
    const prefix = (_a = manager.getPrefix()) !== null && _a !== void 0 ? _a : 'theme';
    const strategy = (_b = manager.getStrategy()) !== null && _b !== void 0 ? _b : strategy_1.Strategy.DataThemeAttribute;
    const name = lodash_1.default.kebabCase(theme.getName());
    const map = {
        [strategy_1.Strategy.Attribute]: () => `[${name}]`,
        [strategy_1.Strategy.PrefixedAttribute]: (prefix) => `[${prefix}-${name}]`,
        [strategy_1.Strategy.DataThemeAttribute]: () => `[data-theme='${name}']`,
        [strategy_1.Strategy.DataAttribute]: () => `[data-${name}]`,
        [strategy_1.Strategy.Class]: () => `.${name}`,
        [strategy_1.Strategy.PrefixedClass]: (prefix) => `.${prefix}-${name}`,
    };
    const selectorize = map[strategy];
    if (!selectorize) {
        throw new Error(errors_1.Errors.UNKNOWN_STRATEGY);
    }
    return selectorize(prefix);
}
exports.getThemeSelector = getThemeSelector;
