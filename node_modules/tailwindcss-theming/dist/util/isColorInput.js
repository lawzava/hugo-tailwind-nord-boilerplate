"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isColorInput = void 0;
const tinycolor_1 = require("@ctrl/tinycolor");
const lodash_1 = __importDefault(require("lodash"));
function isColorInput(colorObject) {
    const objects = [
        ['r', 'g', 'b'],
        ['r', 'g', 'b', 'a'],
        ['h', 's', 'l'],
        ['h', 's', 'l', 'a'],
        ['h', 's', 'v'],
        ['h', 's', 'v', 'a'],
    ];
    return (
    // Check if the color object is an instance of TinyColor
    colorObject instanceof tinycolor_1.TinyColor ||
        // We check if one of the arrays from `objects` has the same keys as
        // the color object
        objects.some(props => lodash_1.default.isEqual(Object.keys(colorObject), props)) ||
        // We check if the color object is in fact a string
        lodash_1.default.isString(colorObject));
}
exports.isColorInput = isColorInput;
