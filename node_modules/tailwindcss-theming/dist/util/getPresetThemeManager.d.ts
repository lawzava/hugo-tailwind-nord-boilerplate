import { ThemeManager } from '../theme/theme';
import { Preset } from '../presets';
/**
 * Gets the Theme Manager of a preset.
 * @param name The preset name.
 */
export declare function getPresetThemeManager(name: Preset): ThemeManager;
