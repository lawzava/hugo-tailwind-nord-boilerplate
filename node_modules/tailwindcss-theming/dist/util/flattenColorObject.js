"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.flattenColorObject = void 0;
const isColorInput_1 = require("./isColorInput");
const lodash_1 = __importDefault(require("lodash"));
/**
 * Flattens the given color object, the Tailwind way.
 * Thanks, Adam.
 *
 * @export
 * @param {ColorObject} colors
 * @returns
 */
function flattenColorObject(colors) {
    const result = lodash_1.default(colors)
        .flatMap((color, name) => {
        if (!lodash_1.default.isObject(color) || isColorInput_1.isColorInput(color)) {
            return [[name, color]];
        }
        return lodash_1.default.map(flattenColorObject(color), (value, key) => {
            const suffix = key === 'DEFAULT' ? '' : `-${key}`;
            return [`${name}${suffix}`, value];
        });
    })
        .fromPairs()
        .value();
    return result;
}
exports.flattenColorObject = flattenColorObject;
